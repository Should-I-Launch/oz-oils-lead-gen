# Frontend Agent Notes

This frontend is a Vite + React SPA. Keep route components under `src/routes`,
shared feature UI under `src/modules`, and primitives under `src/components/ui`.
Use named exports and kebab-case filenames for components.
